
//% icon="\uf03d" color=#1abd56
namespace camera {
    //% block="init camera on TX %tx RX %rx"
    export function setCameraPins(
        tx:SerialPin,
        rx:SerialPin
    ) {
        // to-do
    }
}